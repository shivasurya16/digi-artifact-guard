Digi Artifact Guard test project.

The project contains a plant classification pipeline.

The trained model is stored at:
models/model_v2.pkl

The final checkpoint is:
models/checkpoint_v3.pt

Configuration is stored in:
config/settings.json
